import { Question } from './types';

// Hardcoded data has been removed to support dynamic database content.
// This file is kept to ensure types/imports don't break during transition, 
// but it should no longer be used for exam data.

export const EXAM_CONTEXT = '';
export const QUESTIONS: Question[] = [];